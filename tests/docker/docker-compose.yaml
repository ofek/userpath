version: '3'

services:

  userpath:
    build:
      context: ./../../
      dockerfile: ./${DOCKERFILE}
      args:
        PYTHON_VERSION: ${PYTHON_VERSION}
    volumes:
      - ./../../:/home/userpath
